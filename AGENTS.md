# AGENTS.md

> 适用于本仓库的 AI 开发代理（包括 Codex、代码审查助手、自动修复工具等）。
> 目标：在**不破坏现有架构**的前提下，持续维护、修复、优化本项目，并保持代码风格、目录结构和产品体验一致。

## 0. 使用方式与约束级别

- 本文件是仓库级开发规范，适用于仓库根目录下的全部代码。
- “必须”表示强约束；“应该”表示默认执行，除非有清晰理由；“可以”表示推荐选项。
- 自动生成代码、mock、protobuf、迁移脚本、测试夹具和第三方产物可以例外，但不要手工大改生成文件。
- 若本文件与更具体的目录级约定冲突，优先遵循更具体、更新的约定。

## 1. 项目概览

本项目是一个基于 **Go + Next.js + Mantine** 的全栈工作区，用于影视播放与检索场景，包含：

- 后端：Go，提供 GraphQL + REST 接口
- 前端：Next.js 16 + React 19 + Mantine 8 + ECharts
- 数据库：PostgreSQL
- 任务系统：内置队列 + worker
- 部署：Docker Compose / 本地脚本

项目特点包括媒体检索、在线播放、详情页、收藏、管理员后台、任务队列、封面缓存、字幕管理、维护任务和监控面板等。

## 2. 代理工作原则

修改代码时必须按以下优先级取舍：

1. 保证现有功能不退化
2. 尽量小范围改动
3. 保持架构一致
4. 优先复用现有组件、工具和模式
5. 仅在必要时引入新依赖

默认工作方式：

- 先理解现有结构、入口和调用链，再修改。
- 优先修复根因，不做只遮蔽现象的补丁。
- 避免重构无关代码，避免一次性大范围重写。
- 对不明确的逻辑，先查找同类实现并对齐。
- 保持中英文命名、文案和交互风格统一。
- 修改后必须做与风险匹配的验证；无法完整验证或遇到既有检查债务时必须说明。
- 工作区可能已有用户改动，必须保留无关改动，不能为了“干净 diff”回滚他人工作。

除非明确要求，不要：

- 重构整个项目结构
- 大量改名、迁移目录或更换技术栈
- 引入与当前栈不一致的新框架
- 修改无关模块
- 删除看似冗余但可能被其他模块引用的代码
- 在没有验证的情况下假设行为正确

## 3. 架构与模块边界

### 3.1 后端边界

- `internal/*` 中的业务逻辑应该收敛到 service / usecase 风格的层次，不要把规则散落到 handler、DAO 和 worker 中。
- HTTP / GraphQL handler 负责协议适配、参数解析、响应转换和错误映射，不应该承载复杂业务规则。
- queue / worker 负责调度、幂等和执行上下文，业务判断应该复用 service 层能力。
- 数据库访问必须通过 GORM / gorm-gen DAO 或现有 query 封装完成。
- 业务模块不得直接注入或调用 `pgx`、`pgxpool`、`database/sql`。
- 允许出现底层 SQL / pgx / driver 类型的位置仅限：
  - `backend/internal/database/postgres`
  - `backend/internal/database/migrations`
  - `backend/internal/database/gorm.go`
  - `backend/internal/database/dao`
  - 生成的 model / gql / protobuf 文件
  - `database/sql/driver` 的 Scanner / Valuer 类型
  - 测试、mock、迁移和协议适配代码
- 需要 PostgreSQL 特性时，优先用 GORM 的事务、clause、expression、`Exec` 或 `Raw` 封装在持久层边界内，避免把底层驱动 API 泄漏到业务服务。

### 3.2 前端边界

- `app/`：路由、页面入口和页面级编排。
- `components/`：可复用 UI 组件和页面组件，复杂页面应继续拆分为局部组件。
- `hooks/`：可复用状态、effect 和交互行为。
- `services/`：接口调用、业务服务和数据访问封装。
- `lib/` / `utils/`：纯工具、格式化、转换和通用逻辑。
- 页面层不应该混合大量请求逻辑、格式化逻辑和 UI 状态；复杂逻辑应下沉到组件、hooks 或 services。

### 3.3 目录约定

修改时应优先沿用现有目录职责，不要创造重复语义目录：

- `backend/internal/`：后端业务逻辑、领域逻辑、存储层
- `backend/internal/database/`：数据库连接、DAO、query、迁移适配
- `backend/internal/queue/`：异步任务、队列、调度逻辑
- `frontend/app/`：Next.js 路由与页面入口
- `frontend/src/components/`：前端组件
- `frontend/src/lib/`：前端通用逻辑和接口封装
- `docs/`：如果存在，用于设计说明、变更说明、运维说明

## 4. 文件大小与拆分标准

文件大小限制是维护性指标，不是机械指标。超过软上限必须评估；超过硬上限必须拆分，除非属于明确例外。

| 类型 | 软上限 | 硬上限 | 说明 |
| --- | ---: | ---: | --- |
| Go 普通源文件 | 500 行 | 800 行 | 生成代码、mock、protobuf、测试夹具除外 |
| `frontend/app/` 路由入口 | 200 行 | 500 行 | 只做路由和页面装配 |
| React 功能页容器 | 500 行 | 1000 行 | 页面只保留编排逻辑；复杂播放器等高内聚容器可接近硬上限 |
| React 通用组件 | 400 行 | 800 行 | 超过软上限优先拆出子组件 |
| hooks / utils / 纯 services | 300 行 | 500 行 | 优先按职责拆分 |
| API contract / 类型聚合文件 | 600 行 | 1000 行 | 大量接口类型可集中，但新增逻辑应拆分 |
| 配置 / 常量 / 类型文件 | 400 行 | 800 行 | 大型 schema 或生成类型除外 |

已拆分充分、只承担 hook 编排职责的复杂播放器入口可以作为软上限例外，但仍必须遵守 1000 行硬上限；新增播放器逻辑应继续优先放入 `torrent-player-page.*` 子模块。

拆分优先级：

- 页面逻辑拆到组件
- 业务状态拆到 hooks / stores
- 请求逻辑拆到 services
- 复用逻辑拆到 utils / lib
- 表单逻辑拆到独立模块
- 大型常量配置拆到单独文件
- 数据转换逻辑与 UI 解耦

可以不拆的情况：

- 自动生成文件、mock、protobuf、迁移文件
- 单一高内聚算法或协议实现，拆分会明显降低可读性
- schema、类型声明、测试数据或夹具
- 正在迁移中的文件，但必须避免继续堆叠新职责

## 5. Go 后端规范

- 必须遵循 Go 官方格式和惯例，提交前运行 `gofmt` 或等效格式化。
- 函数职责应该单一，避免过深嵌套，必要时提取辅助函数。
- 错误必须明确处理，不要静默吞掉；对外错误优先使用项目已有错误模型。
- 日志应可读、可追踪，包含必要上下文，避免无意义噪音。
- 数据库查询、任务队列、缓存和外部调用应考虑超时、取消、失败重试和幂等。
- 事务边界必须清晰，跨表写入和队列状态变更要考虑一致性。
- 不要把业务规则散落在多个地方，优先收敛到 service / usecase 层。
- 新增持久化逻辑时必须使用 GORM / gorm-gen DAO，并保持 query 封装可测试。

## 6. 前端 Next.js / React 规范

- 优先使用函数组件与 hooks。
- UI 组件保持可复用、可组合，页面只保留编排逻辑。
- 表单、筛选、列表、详情、播放器控制等模块应按职责拆分。
- 不要把请求逻辑、格式化逻辑、UI 状态混在一个大型组件里。
- 保持 Mantine 的视觉风格统一，交互按钮、tooltip、空状态、加载态、错误态应一致。
- 用户可见改动必须兼顾亮色/暗色模式和响应式布局。
- 新增文案应与现有中英文表达一致，涉及 i18n 时补齐对应资源或映射。

## 7. 数据、接口、队列与缓存

- 任务入队必须考虑幂等性和重复执行。
- 异步任务必须记录足够上下文，便于排查。
- 数据库写入必须考虑事务、一致性和回滚路径。
- 缓存失效与回填逻辑必须明确。
- 封面、字幕、媒体详情等用户可见数据必须处理加载态、空态和失败态。
- 维护任务、清理任务、worker 任务应保持统一入口和统一日志风格。
- 修改接口或数据结构时必须说明兼容性、迁移需求和缓存影响。

## 8. 命名、文案与体验

- 组件名使用 PascalCase。
- 函数、变量、hook 使用 camelCase。
- 常量使用 UPPER_SNAKE_CASE。
- Go 包名保持简短、小写、语义明确。
- 文件名应语义化，避免 `index.tsx` 过度堆积。
- 同一功能在不同页面应保持统一命名，不要出现同一概念多种表达。
- 管理后台提示文案要简洁、明确、可执行，不要把技术术语直接堆给最终用户。

## 9. 修改流程

准备修改代码时应按以下步骤进行：

1. 定位相关入口和调用链。
2. 查找现有类似实现，优先复用模式。
3. 确认模块边界和文件大小是否仍合理。
4. 最小化修改范围。
5. 保证类型、导入、导出关系正确。
6. 补充或更新测试。
7. 运行与改动风险匹配的验证。
8. 如果是用户可见功能，检查文案、空态、加载态、错误态和响应式表现。

## 10. 测试与验证

涉及代码修改时，必须至少做一种有效验证，并优先选择与风险匹配的验证。

常用命令：

- 后端局部测试：`cd backend && go test ./internal/<pkg>`
- 后端全量测试：`cd backend && go test ./...`
- 后端 Task：`cd backend && task test-go`
- 前端类型检查：`cd frontend && npm run typecheck`
- 前端 lint：`cd frontend && npm run lint`（当前历史代码仍有 React Compiler hooks 规则债务，失败时应记录新增/既有问题边界）
- 前端构建：`cd frontend && npm run build`
- 仓库级 Task：`cd backend && task test`、`cd backend && task lint`
- GORM 边界扫描：`rg -n "pgx|pgxpool|database/sql" backend/internal --glob '!**/*_test.go'`
- AGENTS 合规快检：`./scripts/check-agents-compliance.sh`

验证选择建议：

- 修复 bug：优先补充或运行回归测试。
- 新增业务逻辑：覆盖核心成功路径和主要失败路径。
- 修改持久层、队列或缓存：运行相关 Go 包测试，并检查事务、幂等和重试行为。
- 修改前端页面或组件：至少运行 `npm run typecheck`；用户可见改动优先补充 lint / build / 手工验证。若 lint 因既有 React hooks 规则债务失败，应确认本次改动没有新增无关错误，并在结果中说明。
- 仅文档改动：至少检查文档内容与当前代码、命令和目录一致。

如果无法完整验证，必须说明已验证内容、未验证内容和剩余风险，不要假装验证成功。

## 11. 文档与依赖

- 重要功能变化、架构变化、行为变化，应同步更新相关文档。
- 可更新位置包括 `README.md`、`AGENTS.md`、存在的 `docs/` 文档、配置示例、迁移说明和运维说明。
- 不要假设 `docs/` 一定存在；不存在时更新最接近的现有文档。
- 优先使用项目已有依赖；新依赖必须有明确收益。
- 避免重复引入功能相近的库，能用标准库或现有工具链解决的不要增加复杂度。

## 12. 完成后的输出建议

完成变更后，最好说明：

- 修改了哪些文件
- 每个文件的主要变化
- 是否影响接口或数据结构
- 是否需要迁移或清缓存
- 如何验证
- 是否存在已知风险

## 13. 默认保守策略

遇到以下情况，应优先保守处理：

- 信息不足
- 代码路径不清晰
- 存在多个实现候选
- 修改可能影响数据库或任务队列
- 涉及用户数据、媒体数据、下载缓存或播放状态

保守策略包括：

- 先定位问题范围
- 先做最小修复
- 先保证兼容
- 先加保护再优化
- 先补日志再做复杂重构

## 14. 最后一条原则

在本仓库里，**稳定、可读、可维护** 永远优先于“看起来聪明”的实现。
